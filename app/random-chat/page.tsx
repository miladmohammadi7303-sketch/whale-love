'use client';
import Link from 'next/link';import {useState} from 'react';
export default function RandomChat(){const [searching,setSearching]=useState(false);const [matched,setMatched]=useState(false);function start(){setSearching(true);setMatched(false);setTimeout(()=>{setSearching(false);setMatched(true)},1800)}return <main className="shell page"><nav className="nav"><Link href="/discover" className="brand gold">Whale Love</Link><Link className="btn" href="/chat">چت‌ها</Link></nav><section className="glass center" style={{padding:'70px 24px',minHeight:'65vh',display:'grid',placeItems:'center'}}><div><div style={{fontSize:70}}>🎲</div><h1 className="brand" style={{fontSize:42}}>Random Chat</h1><p className="muted">با یک نفر جدید، کاملاً تصادفی آشنا شو.</p>{searching?<><div className="gold" style={{fontSize:18,marginTop:30}}>در حال پیدا کردن یک نفر...</div><p className="muted">صف تصادفی فعال است</p></>:matched?<><div className="gold" style={{fontSize:20,marginTop:30}}>✨ Match پیدا شد!</div><div className="actions" style={{justifyContent:'center'}}><Link className="btn primary" href="/chat">ورود به چت</Link><button className="btn" onClick={start}>نفر بعدی</button></div></>:<button className="btn primary" style={{marginTop:28}} onClick={start}>شروع Random Chat</button>}</div></section></main>}
